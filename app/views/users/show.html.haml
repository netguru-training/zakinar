- if notice
  %p#notice= notice
  %p#error= flash[:error]

.row
  .col-sm-4.col-xs-12
    .show_data
      .title= "#{@user.first_name} #{@user.last_name}"

  .col-sm-8.col-xs-12
    .show_data
      .my_profile
        .glyphicon.glyphicon-user
          %p
            %b e-mail:
            = @user.email
          %p
            %b last logged in:
            = distance_of_time_in_words(@user.current_sign_in_at, Time.now) + " ago"
.container
  .row
    - @user.notes.each_slice(4) do |row|
      .row
        - row.each do |note|
          .col-lg-3.col-md-3.col-sm-6.col-xs-12
            .category_container
              .name
                %h2= link_to note.title, note
                %h3= note.description
                %h4 Utworzono dnia:
                %h4= note.created_at.strftime("%d-%m-%Y")
